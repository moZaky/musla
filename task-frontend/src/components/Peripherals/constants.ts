import { PeripheralsModel } from "../models/peripheral-model";

export const defaultNewPeripheral: PeripheralsModel = {
  
    record_status:"n",//new record
    gateway:"",
    status:"OFFLINE",
    uid:0,
    vendor:""

} 